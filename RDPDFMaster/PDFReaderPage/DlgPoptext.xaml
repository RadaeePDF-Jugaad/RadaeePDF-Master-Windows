<UserControl
    x:Class="RDPDFReader.annotui.DlgPoptext"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RDPDFReader.PDFReaderPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="210"
    d:DesignWidth="280">

    <RelativePanel Padding="10,10,10,10" Background="#FFE0E0E0">
        <TextBlock x:Name="labTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Text="Text Selection" FontSize="20" Height="32" />
        <TextBlock x:Name="labLabel" RelativePanel.Below="labTitle" Text="Label:" Width="60" Margin="0,5,0,0"/>
        <TextBox x:Name="txtLabel" RelativePanel.RightOf="labLabel" RelativePanel.Below="labTitle" Width="200"/>
        <TextBlock x:Name="labPopup" RelativePanel.Below="txtLabel" Text="Content:" Width="60" Margin="0,5,0,0"/>
        <ScrollViewer RelativePanel.RightOf="labPopup" RelativePanel.Below="txtLabel" Width="200" Height="120">
            <TextBox x:Name="txtPopup" TextWrapping="Wrap"/>
        </ScrollViewer>
    </RelativePanel>
</UserControl>
