<UserControl
    x:Class="RDPDFReader.annotui.RichMediaControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RDPDFReader.annotui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="#FFCCCCCC">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <MediaElement x:Name="mPlayer" Grid.Column="0"></MediaElement>
        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,-200,0">
            <!--Button x:Name="backBtn" Style="{StaticResource SkipBackAppBarButtonStyle}" VerticalAlignment="Center" Tapped="backBtnTapped"/-->
            <Button x:Name="mPlayBtn" Tag="0" Style="{StaticResource AppBarButtonStyle}" Tapped="onItemTapped">
                <Image Source="/Assets/images/richmedia_play.png"/>
            </Button>
            <Button x:Name="mPauseBtn" Style="{StaticResource AppBarButtonStyle}" Tapped="onItemTapped">
                <Image Source="/Assets/images/richmedia_pause.png"/>
            </Button>
            <Button x:Name="mStopBtn" Style="{StaticResource AppBarButtonStyle}" Tapped="onItemTapped">
                <Image Source="/Assets/images/richmedia_stop.png"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
